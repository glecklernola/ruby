scores = [100, 80, 75, 93]
sum = 0
avearg = 0

scores.each do |score|
    sum += score
average = sum / scores.length

puts "The sum of #{scores} is # #{average}
end

puts "The sum of #{scores} is #{sum}. The average of the scores is #{sum}/scores.length}.""
